kind: Service
apiVersion: v1
metadata:
  name: capstone-sample-app-service
  labels:
    app: capstone-sample-app
spec:
  ports:
    - port: 80
      targetPort: "http-server"
  selector:
    app: capstone-sample-app
  type: LoadBalancer
